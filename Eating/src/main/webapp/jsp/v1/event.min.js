/**
* --------------------------------
* EVENT JS
* --------------------------------
*/

$(window).on('load', function(){
    var path = $("#wrap").attr('class').split(" ") || "";

	switch (path[1]) {
        //detail
        case 'detail' :
            
        break;
	}
});